[theme]
# 이 앱은 커스텀 카드(흰 배경) CSS만 있고 다크모드 대응 CSS는 없다.
# OS/브라우저가 다크모드면 Streamlit 네이티브 UI만 어두워지고 커스텀 카드는
# 그대로 흰 배경이라, 흰 글씨(다크모드 기본 글자색)와 흰 배경이 겹쳐 안 보인다.
# 그래서 OS 설정과 무관하게 항상 라이트 테마로 고정한다.
base = "light"
