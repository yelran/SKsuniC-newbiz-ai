streamlit==1.38.0
pandas==2.2.2
openpyxl==3.1.5

python-pptx        
pdfplumber         
reportlab>=4.0,<5 

openai>=2.0,<3
python-dotenv>=1.0,<2       

# torch / sentence-transformers는 일부러 뺐다.
# import만으로 RSS가 700MB를 넘어(실측) 512MB 배포 환경에서 프로세스가 죽는다.
# 요구역량 매핑은 F4-1의 LLM 경로가 대신하고, 모델이 없으면 자동으로 그쪽을 탄다.
# 메모리가 넉넉한 곳에서 임베딩을 쓰고 싶으면 아래를 직접 설치하고
# F4_1_USE_EMBEDDING=1 을 설정한다.
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu \
#       "torch>=2.2,<3" "sentence-transformers>=3.0,<6"
